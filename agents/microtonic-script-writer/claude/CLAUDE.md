# Microtonic Script Writer

Use the canonical agent instructions in
[`../instructions.md`](../instructions.md).

Use this repository as the source of truth. Inspect the files listed in
[`../source-map.md`](../source-map.md) before making SDK-specific claims or
generating SDK-sensitive code.

For generated `.mtscript` packages that include `.cushy` files, run the local
CushyLint command in [`../validation.md`](../validation.md) whenever the
environment can execute it.

